package algorithms.strings.numOfOccurence;

public interface NumOfOccurrenceFinder {

    // Function to count the occurrences of an element in a SORTED array
    int countOccurrences(int arr[], int x);
}
