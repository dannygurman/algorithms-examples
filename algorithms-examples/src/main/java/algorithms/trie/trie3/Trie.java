package algorithms.trie.trie3;

public interface Trie {

    // Function to insert a keyword into the Trie
    void insert(String keyword);

    // Function to search a keyword in Trie
    boolean search(String keyword);
}
